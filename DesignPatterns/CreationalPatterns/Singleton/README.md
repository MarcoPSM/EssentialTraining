# Example of implementing the Singleton design pattern. 

Example of the Singleton pattern for C++
This is an implementation of a thread safe singleton

### How to run 
```bash
g++ -o singleton main.cpp && ./strategy
```
